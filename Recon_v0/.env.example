# ===== NextAuth (Google OAuth) =====
# Create credentials at https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Required for production; recommended for local dev too.
# Generate with e.g. `openssl rand -base64 32`
NEXTAUTH_SECRET=

# Your app URL
NEXTAUTH_URL=http://localhost:3000

# Auth.js (NextAuth v5) strict host checking can break `next build`/`next start` locally.
# If you prefer env-based config, set this to true.
AUTH_TRUST_HOST=true

# ===== TMDB (server-only) =====
# Used by Next.js route handlers under /app/api/movies/*.
# Keep this secret; do NOT prefix with NEXT_PUBLIC.
TMDB_API_KEY=

# ===== Backend (Flask) =====
# Optional. If set, Next.js will proxy /api/movies/* to your Flask server
# instead of using the built-in TMDB route handlers.
# Leave blank unless you are running the Flask backend.
BACKEND_URL=

# ===== Supabase (public) =====
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ===== Supabase (server-only) =====
# Required for server-side writes (NextAuth user sync + watchlist API).
# Keep this secret; do NOT prefix with NEXT_PUBLIC.
SUPABASE_SERVICE_ROLE_KEY=
